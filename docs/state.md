# state

**Phase:** 0 — one story on a phone. **Built and deployed** (temporary Vercel URL, needs claiming).
**What works:** listen mode (karaoke on real word timestamps, magic-word panel, not-yet modelled blend, skip, moral, done), read mode (audio off, tap the pink word, Next gated), real IPA phoneme clips, PWA manifest + service worker, 23 unit/contract tests.
**Next step:** owner claims the Vercel deployment (or runs `vercel login`), installs on iPhone + Android, finishes The Fox and the Crow on both. Then: GPT-5.6 review of plan + build (Codex limit resets 2026-09-11 01:04), Phase 1 (persistence, parent settings, attempt log).
**Resume rule:** on a 5-hour usage limit or an explicit "pause", resume from this file, up to 3 consecutive sessions.

node_modules
dist
dev-dist
.vercel
# secrets (DEEPINFRA_API_KEY etc.) — plan-voice.md A1 assumes this is already ignored; it was not
.env.local
# deploy-time audio artefacts (regenerate with scripts/gen-audio.py)
public/library/
public/prompts/
# recorded phoneme clips ARE committed; placeholders are regenerated
!public/sounds/
*.log
.DS_Store
design/storytime-redesign.html
docs/demo/

# recorded sound packs we are not licensed to redistribute (see scripts/sound-packs.json)
assets/packs/*
!assets/packs/soundcity/
